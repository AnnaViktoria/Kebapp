<link href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/4.7.0/css/font-awesome.css" rel="stylesheet"/>

<div class="kebapp-card">
  <div class="kebabcard-content" style="background-image: linear-gradient(rgba(0,0,0,0.2) 0, rgba(0,0,0,0.2) 50%, rgba(251,117,37,0.2) 100% ), url('<%= cl_image_path kebab.photo, height: 300, width: 400, crop: :fill %>')">
    <div class="text-center">
      <label for="card<%= kebab.id %>" class="button" aria-hidden="true">
        <h2 class="kebapp-store-name"><strong><%= kebab.name %></strong></h2>
        </label>
          <div class="card-rating d-flex justify-content-center">
                    <% (1..5).each do |value|%>
                      <% if value <= kebab.rating %>
                        <i class="fas fa-star"></i>
                      <% else %>
                        <i class="fa fa-star-o"></i>
                      <% end %>
                    <% end %><!-- <i class="fas fa-star-half-alt"></i> -->
        </div>
            <div class="kebapp-card-text">
              <p class="kebapp-card-hours">Opening Hours: <%= kebab.opening_hours_today %></p>
              <p class="kebapp-card-distance">500m away</p>
                </div>
      </div>
    </div>
  </div>
</div>





